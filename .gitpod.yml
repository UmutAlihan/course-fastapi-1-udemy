ports:
  - port: 5000
    onOpen: open-preview

tasks:
#  - before: |
#      export FLASK_APP=flaskr
#      export FLASK_ENV=development
    init: |
      pip install -e .
      pip install -r backend/requirements.txt
      docker-compose up -d
#    command: |
#      flask run

vscode:
  extensions:
    - ms-python.python